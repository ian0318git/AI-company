FROM node:20-alpine

ARG VITE_API_HOST=127.0.0.1
ENV VITE_API_HOST=$VITE_API_HOST

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --quiet

COPY . .
RUN npm run build

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
